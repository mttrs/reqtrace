{
  "name": "Request trace",
  "repository": "https://github.com/mttrs/reqtrace",
  "addons": [
    {
      "plan": "papertrail:choklad"
    }
  ],
  "buildpacks": [
    { "url": "heroku/go" }
  ],
  "environments": {
    "test": {
      "addons": ["heroku-postgresql:in-dyno"],
      "env": {
        "HEROKU_CI_POSTGRES_FSYNC_OFF": "off"
      },
      "scripts": {
	"test-setup": "bash ./db-setup.sh",
	"test": "go test"
      }
    }
  }
}
