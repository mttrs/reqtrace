{
  "name": "Request trace",
  "repository": "https://github.com/mttrs/reqtrace",
  "addons": [
    {
      "plan": "papertrail:choklad"
    }
  ],
  "buildpacks": [
    { "url": "heroku/go" }
  ],
  "environments": {
    "test": {
      "addons": ["heroku-postgresql:in-dyno"],
      "scripts": {
	"test": "psql $DATABASE_URL -c 'SHOW max_connections;' && go test"
      }
    }
  }
}
